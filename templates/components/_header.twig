{% if skin_theme_options.finalium_icons.use_hitchhiker_icons %}
    {% set masthead_button_icon_size = 24 %}
    {% set masthead_guide_icon_size = 16 %}
{% else %}
    {% set masthead_button_icon_size = 20 %}
    {% set masthead_guide_icon_size = 20 %}
{% endif %}

<div id="masthead-container" class="{% if banner_text %}with-fullscreen-banner{% endif %}">
    <div id="masthead">
        <div id="header-main">
            <div id="logo-container">
                <div class="button button-default-invis masthead-button guide-button" id="guide-toggle">
                    {{ icon(fi.masthead_guide, masthead_guide_icon_size) }}
                </div>
                <a href="/" class="spf-link">
                    {% apply spaceless %}
                    <img id="logo" src="{{ logo }}" alt="{{ website_branding.name }}">
                    {% if options.finalium_enable_spf %}
                        <span class="content-region" style="color:red;">SPF</span>
                    {% elseif country_code is defined and country_code|trim != '' and country_code != 'US' and skin_theme_options.finalium_header.hitchhiker %}
                        <span class="content-region">{{ country_code }}</span>
                    {% endif %}
                    {% endapply %}
                </a>
            </div>
            <form id="masthead-search" action="/search">
                <div id="masthead-search-terms">
                    <label><input type="text" name="query" {% if not skin_theme_options.finalium_header.hide_search_placeholder %}placeholder="{{ localize('search') }}"{% endif %}></label>
                </div>
                <button id="search-btn" class="button button-default">{{ icon(fi.masthead_search, 15) }}</button>
            </form>
        </div>
        {% if logged_in %}
            <div id="header-user">
                {% if skin_theme_options.finalium_header_upload_button_icon %}
                    <a class="button button-default-invis masthead-button uix-tooltip" title="{{ localize('new_upload') }}" href="/upload">{{ icon(fi.masthead_upload, masthead_button_icon_size) }}</a>
                    <a class="button button-default-invis masthead-button" href="/notifications">{{ icon(fi.masthead_bell, masthead_button_icon_size) }}</a>
                {% else %}
                    <a href="/upload" class="button button-default">{{ localize('new_upload') }}</a>
                    <a class="button button-default-invis masthead-button" href="/notifications">{{ icon(fi.masthead_bell, masthead_button_icon_size) }}</a>
                {% endif %}
                <div id="masthead-loggedin" class="masthead-user">
                    <div class="menu-button user-menu-button" data-menu-id="user-menu">
                        <img class="profile-picture img-fluid" src="{{ profile_picture(user_data.id) }}" alt="{{ user_data.name }}">
                    </div>
                    <div id="user-menu">
                        <div class="header">{{ account_data.email }}</div>
                        <div class="body">
                            <img class="profile-picture img-fluid" src="{{ profile_picture(user_data.id) }}" alt="{{ user_data.name }}">
                            <div class="info">
                                <div class="name">{{ user_data.title }}</div>
                                <div class="followers">{% if user_stat_data.followers != 0 %}{{ localize('followers', user_stat_data.followers) }}{% endif %}</div>
                                <div class="buttons">
                                    <a class="button button-default" href="/my_uploads">{{ localize('manager') }}</a>
                                    <a class="button button-default settings" href="/settings" title="{{ localize('account_settings') }}">{{ icon(fi.account_settings, account_settings_button_icon_size) }}</a>
                                </div>
                            </div>
                        </div>
                        <div class="user-menu-multilogin">
                            {#<div class="header">Other accounts</div>#}
                            {% for link in header_user_switcher_links() %}
                                <a class="user-option" href="/login/{{ link.name }}">
                                    <img src="{{ profile_picture(link.id) }}" class="profile-picture" alt="{{ link.title }}" loading="lazy">
                                    <div class="info">
                                        <div class="name">{{ link.title }}</div>
                                        <div class="followers">{{ localize('followers', link.f_index) }}</div>
                                    </div>
                                </a>
                            {% endfor %}
                        </div>
                        <div class="user-menu-footer">
                            {% if user_data.powerlevel > 2 %}<a href="/dashboard" class="button button-destructive">{{ localize('dashboard') }}</a>{% endif %}
                            <a class="button button-default" href="/logout">{{ localize('logout') }}</a>
                        </div>
                    </div>
                </div>
            </div>
        {% else %}
            <div id="header-signin">
                {% if skin_theme_options.finalium_header.hitchhiker %}
                    {% if skin_theme_options.finalium_header_upload_button_icon %}
                        <a class="button button-default-invis masthead-button uix-tooltip" title="{{ localize('new_upload') }}" href="/upload">{{ icon(fi.masthead_upload, masthead_button_icon_size) }}</a>
                    {% else %}
                        <a href="/upload" class="button button-default">{{ localize('new_upload') }}</a>
                    {% endif %}
                    <a href="/login" class="button button-primary">{{ localize('login') }}</a>
                {% else %}
                    <a href="/login">{{ localize('login') }}</a>
                    <a href="/register">{{ localize('register') }}</a>
                {% endif %}
            </div>
        {% endif %}
    </div>
</div>
{# TODO: if there was only one comment, the title would only say "COMMENT", rather than "COMMENTS" #}
<div class="comments-header"><span class="title">{{ localize("comments") }}</span> • {{ count }}</div>

{% if logged_in %}
    <div class="comment" id="new-comment">
        <div class="comment-avatar">
            <a href="/user/{{ comment.author.info.username }}" class="spf-link">
                <img src="{{ profile_picture(user_data.id) }}" class="profile-picture" alt="{{ user_data.name }}" loading="lazy">
            </a>
        </div>
        <div class="comment-body">
            <textarea class="form-control" id="commentContents" rows="3" placeholder="{{ localize('write_comment') }}"></textarea>
            <div class="comment-form-buttons">
                <button id="post" class="button button-primary">{{ localize('post_action') }}</button>
            </div>
        </div>
    </div>
{% endif %}
<div id="comment"></div>
{% for comment in comments %}
    {{ comment(comment) }}
{% endfor %}